[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:starlette.*
markers =
    slow: tests that hit a real model or network (excluded from CI by default)
